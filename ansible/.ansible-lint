---
# The collection passes ansible-lint's "production" profile clean, which is
# the strictest built-in profile. Advertise that: a green CI badge at
# `production` is a stronger portfolio signal than a green badge at `min`.
profile: production

exclude_paths:
  - tests/
  - rdp_artifacts/
