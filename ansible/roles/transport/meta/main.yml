galaxy_info:
  author: Adrian Estrada
  description: Resolves which transport (direct/ssm/iap/bastion) carries a host's WinRM connection.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: CentOS
      versions:
        - all
    - name: Windows
      versions:
        - all
  galaxy_tags:
    - winrm
    - tunnel
    - transport
